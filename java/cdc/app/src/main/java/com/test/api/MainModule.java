package com.test.api;

import com.google.inject.AbstractModule;
//import com.test.api.logger.LoggerModule;

public class MainModule extends AbstractModule {
    @Override
    protected void configure() {
//        install(new LoggerModule());
    }
}
